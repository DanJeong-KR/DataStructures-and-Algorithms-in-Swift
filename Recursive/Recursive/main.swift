import Foundation
/*
example(description: "Factorial") {
    print("재귀 호출에서 0! 의 값은 \(factorial_recur(n: 0))")
    print("반복문에서 0! 의 값은 \(factorial_for(n: 0))")
    print("재귀 호출에서 3! 의 값은 \(factorial_recur(n: 3))")
    print("반복문에서 3! 의 값은 \(factorial_recur(n: 3))")
    print("재귀 호출에서 20! 의 실행시간은 \(processTime { factorial_recur(n: 20) })")
    print("반복문에서 20! 의 실행시간은 \(processTime { factorial_for(n: 20) })")
}
*/

/*
example(description: "Power") {
    print("재귀 호출에서 2 의 0 제곱 의 값은 \(power_recur(x: 2, n: 0))")
    print("반복문 에서 2 의 0 제곱 의 값은 \(power_for(x: 2, n: 0))")
    print("재귀 호출에서 2 의 3 제곱 의 값은 \(power_recur(x: 2, n: 3))")
    print("반복문 에서 2 의 3 제곱 의 값은 \(power_for(x: 2, n: 3))")
    print("재귀 호출에서 2 의 200 제곱 의 실행시간은 \(processTime {power_recur(x: 2, n: 200)})")
    print("반복문 에서 2 의 200 제곱 의 실행시간은 \(processTime {power_for(x: 2, n: 200)})")
}
 */

/*
example(description: "Fibonacci") {
    print("재귀 호출에서 피보나치 수열의 0번째 값은 \(fibonacci_recur(n: 0))") 
    print("반복문 에서 피보나치 수열의 1번째 값은 \(fibonacci_for(n: 1))") 
    print("배열기반 반복문 에서 피보나치 수열의 1번째 값은 \(fibonacci_for_array(n: 1))") 
    
    print("재귀 호출에서 피보나치 수열의 3번째 값은 \(fibonacci_recur(n: 3))")
    print("반복문 에서 피보나치 수열의 4번째 값은 \(fibonacci_for(n: 4))")
    print("배열기반 반복문 에서 피보나치 수열의 5번째 값은 \(fibonacci_for_array(n: 5))")
}
*/
print("\n")
/*
example(description: "Fibonacci 수행속도") {
    print("재귀 호출에서 10번째 값을 계산하는 수행속도는 \(processTime { fibonacci_recur(n: 10) })")
    print("반복문 에서 10번째 값을 계산하는 수행속도는 \(processTime { fibonacci_for(n: 10) })")
    print("배열기반 반복문 10번째 값을 계산하는 수행속도는 \(processTime { fibonacci_for_array(n: 10) })")
    
    print("재귀 호출에서 43번째 값을 계산하는 수행속도는 \(processTime { fibonacci_recur(n: 43) })")
    print("반복문 에서 43번째 값을 계산하는 수행속도는 \(processTime { fibonacci_for(n: 43) })")
    print("배열기반 반복문 43번째 값을 계산하는 수행속도는 \(processTime { fibonacci_for_array(n:43) })")
}
*/
/*
example(description: "Hanoi") {
    hanoi(diskNum: 4, from: "a", to: "b", by: "c")
}*/


//processTime { let fibo = fibonacci_recur(n: 43) }
//print(processTime { fibonacci_recur(n: 43) })

/*

processTime(description: "피보나치 반복법") {
    let fibo = fibonacci_for(n: 90)
}
*/
/*

example(description: "LCM") {
    print(" 18 와 12 의 최소 공배수는 \(lcm(a: 18, b: 12)) ")
    print(" 12345 와 1234 의 최소 공배수는 \(lcm(a: 12345, b: 1234)) ")
}
*/
/*
 var a = 12
 var b = 18
 let gcdFor = gcd_for(a: &a, b: &b)
 print(gcdFor)
 let lcmValue = lcm(a: 18, b: 12)
 print(lcmValue)
 */
